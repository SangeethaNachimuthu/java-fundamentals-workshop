Pseudocode

START
    REPEAT
        READ number1
        READ number2
        READ desiredOperation
        SWITCH 
            case a: perform addition and PRINT the result
            case s: perform subtraction and PRINT the result
            case m: perform multiplication and PRINT the result
            case d: perform division and PRINT the result
            case e: PRINT Exit Calculator App
            default: PRINT choose correct operation
        END SWITCH
    END REPEAT
END
